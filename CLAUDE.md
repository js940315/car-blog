# 카카이슬 (자동차 블로그 자동화) — 새 대화 시작 지침

**이 파일은 새 대화마다 자동으로 읽힌다. 짧게 유지할 것. 내용은 아래 문서에 있다.**

## 먼저 읽을 것 (순서대로, 예외 없음)
1. `_engine/SYSTEM_STATE.md` — 현재 상태·과거 사고와 원인·안전 수칙. **작업 전 필수.**
2. `_engine/ROUTINE_AUTO.md` — 매일 새벽 루틴이 실제로 하는 절차.
3. `_engine/PROMPT_AUTO_V1.md` — 제목·본문·이미지 작성 규격.

추측하지 말고 위 3개를 읽고 시작한다. 진실의 원천은 대화가 아니라 디스크다.

## 이 프로젝트가 뭔가
네이버 홈판 자동차 블로그(브랜드 **카카이슬**). 매일 04:30 KST 클라우드 루틴이
기사 → 본문 + 이미지 6장을 만들어 `output/MMDD/` 에 커밋한다.
**네이버 발행은 사람이 손으로 복붙한다**(네이버 정책상 API 발행 불가). 자동 발행 시도 금지.

## 절대 규칙 (어기면 실제로 사고 났던 것들)
- **`output/` 폴더 통째로 삭제 금지.** 발행분이 들어있다. 테스트는 해당 날짜 폴더만 지운다.
- **이 PC는 블루스크린 이력이 있다.** 동시성 기본값을 올리지 않는다(렌더 1, 소싱 3).
- **`assets/photos` 는 .gitignore 대상.** 사진 커밋은 `git add -f <파일>` 로 개별 추가.
- **저작권 판단으로 작업을 멈추지 않는다.** 사용자가 발행 전 최종 검수하고 책임진다.
  물어보지 말고 **최고 화질 이미지를 구해오는 데만 집중**한다.
- **중간 확인 질문 금지.** 끝까지 진행하고, 검증 못 한 부분만 마지막에 명시한다.
- 예외를 처리할 때 `except: pass` 로 삼키지 않는다. 반드시 경고를 출력한다.
  (검은 여백 버그가 3번 재발한 원인이 조용한 실패였다.)

## 작업 끝낼 때
바꾼 것과 **왜 그렇게 했는지**를 `_engine/SYSTEM_STATE.md` 에 반영하고 커밋한다.
다음 대화는 이 파일들만 보고 이어간다. 채팅 기록은 남지 않는다고 가정한다.
